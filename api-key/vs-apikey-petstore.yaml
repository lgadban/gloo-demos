apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: petstore-apikey
  namespace: gloo-system
spec:
  virtualHost:
    domains:
    - '*'
    options:
      extauth:
        configRef:
          name: apikey-auth
          namespace: gloo-system
    routes:
    - matchers:
      - prefix: /
      routeAction:
        single:
          upstream:
            name: default-petstore-8080
            namespace: gloo-system
