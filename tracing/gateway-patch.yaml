spec:
  httpGateway:
    options:
      httpConnectionManagerSettings:
        tracing:
          requestHeadersForTags:
          - ":path"
          - origin
          tracePercentages:
            overallSamplePercentage: 100
          verbose: true

